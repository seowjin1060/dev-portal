import { shcCustomPluginSamplePlugin } from './plugin';

describe('shc-custom-plugin-sample', () => {
  it('should export plugin', () => {
    expect(shcCustomPluginSamplePlugin).toBeDefined();
  });
});
